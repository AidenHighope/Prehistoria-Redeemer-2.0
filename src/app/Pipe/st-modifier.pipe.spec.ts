import { StModifierPipe } from './st-modifier.pipe';

describe('StModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new StModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
